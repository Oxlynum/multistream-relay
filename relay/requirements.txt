fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# PyAV — used by bpm_inject.py to append Broadcast Performance Metrics SEI to the
# Twitch eRTMP HEVC passthrough. Bundles its own libav, so it only ever touches
# pipe+mpegts (jellyfin-ffmpeg keeps the SRT-in / eRTMP-out network I/O).
av==15.1.0
